# Client-side dependencies only. The model and vLLM run in the container (docker-compose.yml).
requests
Pillow      # image handling; also used to encode rasterized PDF pages and for --max-side
pypdfium2   # only needed for PDF input (permissive Apache/BSD license, no AGPL)
